<form method="post" action="{{ path('app_audio_file_delete', {'id': audio_file.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ audio_file.id) }}">
    <button class="btn">Delete</button>
</form>
